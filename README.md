
# Rotational Barrier Scans

- Altering the placement of files in the `barrier_fitting` folder may disrupt 
the following workflow (adjust file paths in scripts accordingly).
- Adjust ORCA input parameters in `createInput.py` and execute to generate input & batch files to perform rotational barrier scans on a HPC. It may be easier to do this locally and transfer files to HPC storage due to package requirements.
- Adjust commands used to submit batch files and execute `runBatch.py` on a HPC.

